[build]
builder = "nixpacks"
watchPatterns = ["**"]

[deploy]
startCommand = "npm start"

# Environment variables
[deploy.env]
NODE_ENV = "production"
RAILWAY_VOLUME_MOUNT_PATH = "/data"

# Volume configuration for persistent database storage
[[deploy.volumes]]
mountPath = "/data"
name = "paintpro-database"